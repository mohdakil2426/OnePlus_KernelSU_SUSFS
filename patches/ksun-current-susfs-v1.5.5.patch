diff --git a/kernel/Kbuild b/kernel/Kbuild
--- a/kernel/Kbuild
+++ b/kernel/Kbuild
@@ -3,6 +3,7 @@ kernelsu-objs := core/init.o
 kernelsu-objs += compat/kernel_compat.o
 kernelsu-objs += feature/kernel_umount.o
+kernelsu-objs += feature/susfs_legacy.o
 kernelsu-objs += feature/sulog.o
 kernelsu-objs += feature/sucompat.o
diff --git a/kernel/Kconfig b/kernel/Kconfig
--- a/kernel/Kconfig
+++ b/kernel/Kconfig
@@ -58,4 +58,95 @@ config KSU_KPROBES_HOOK
 	  Enable KPROBES, KRETPROBES and TRACEPOINT hook for KernelSU core.
 	  This should not be used on kernel below 5.10.
 
+config KSU_SUSFS
+	bool "SUSFS support"
+	depends on KSU
+
+config KSU_SUSFS_HAS_MAGIC_MOUNT
+	bool "SUSFS magic-mount support"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_SUS_PATH
+	bool "Hide paths"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_SUS_MOUNT
+	bool "Hide mounts"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT
+	bool "Automatically hide KernelSU mounts"
+	depends on KSU_SUSFS_SUS_MOUNT
+
+config KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT
+	bool "Automatically hide bind mounts"
+	depends on KSU_SUSFS_SUS_MOUNT
+
+config KSU_SUSFS_SUS_KSTAT
+	bool "Spoof file metadata"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_SUS_OVERLAYFS
+	bool "Hide overlayfs metadata"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_TRY_UMOUNT
+	bool "Unmount selected paths for app processes"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT
+	bool "Automatically add bind mounts to unmount list"
+	depends on KSU_SUSFS_TRY_UMOUNT
+
+config KSU_SUSFS_SPOOF_UNAME
+	bool "Spoof uname"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_ENABLE_LOG
+	bool "Enable SUSFS kernel logging"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS
+	bool "Hide KernelSU and SUSFS symbols"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
+	bool "Spoof command line"
+	depends on KSU_SUSFS
+
+config KSU_SUSFS_OPEN_REDIRECT
+	bool "Redirect selected paths"
+	depends on KSU_SUSFS
+
 endmenu
diff --git a/kernel/core/init.c b/kernel/core/init.c
--- a/kernel/core/init.c
+++ b/kernel/core/init.c
@@ -5,6 +5,9 @@
 #include <linux/rcupdate.h>
 #include <linux/sched.h>
 #include <linux/workqueue.h>
+#ifdef CONFIG_KSU_SUSFS
+#include <linux/susfs.h>
+#endif
 
 #include "policy/allowlist.h"
@@ -94,6 +97,10 @@ int __init kernelsu_init(void)
 
 	ksu_supercalls_init();
 
+#ifdef CONFIG_KSU_SUSFS
+	susfs_init();
+#endif
+
 	
 
 	if (ksu_late_loaded) {
diff --git a/kernel/compat/kernel_compat.h b/kernel/compat/kernel_compat.h
--- a/kernel/compat/kernel_compat.h
+++ b/kernel/compat/kernel_compat.h
@@ -88,7 +88,9 @@ static inline void ksu_kvfree(const void *buf)
 // https://elixir.bootlin.com/linux/v4.14.222/source/lib/string.c#L282
 static inline ssize_t __strscpy_pad(char *dest, const char *src, size_t count)
 {
-#if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 14, 222)
+/* OnePlus 4.19 did not merge the stable strscpy_pad backport despite its
+ * newer version number. Keep the local implementation for pre-5.10 trees. */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 10, 0)
     return strscpy_pad(dest, src, count);
 #elif LINUX_VERSION_CODE >= KERNEL_VERSION(4, 3, 0)
     ssize_t res = strscpy(dest, src, count);
diff --git a/kernel/feature/kernel_umount.c b/kernel/feature/kernel_umount.c
--- a/kernel/feature/kernel_umount.c
+++ b/kernel/feature/kernel_umount.c
@@ -82,6 +82,14 @@ static void try_umount(const char *mnt, int flags)
     ksu_umount_mnt(mnt, &path, flags);
 }
 
+#ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
+void ksu_try_umount(const char *mnt, bool check_mnt, int flags, uid_t uid)
+{
+	(void)check_mnt;
+	(void)uid;
+	try_umount(mnt, flags);
+}
+#endif
+
 struct umount_tw {
 	struct callback_head cb;
 };
diff --git a/kernel/feature/selinux_hide.c b/kernel/feature/selinux_hide.c
--- a/kernel/feature/selinux_hide.c
+++ b/kernel/feature/selinux_hide.c
@@ -17,7 +17,7 @@
 #include "selinux/selinux.h"
 #include "feature/selinux_hide.h"
 
-#if defined(CONFIG_KPROBES)
+#if defined(CONFIG_KSU_KPROBES_HOOK)
 extern struct kprobe *init_kprobe(const char *name, int (*pre_handler)(struct kprobe *, struct pt_regs *));
 extern void destroy_kprobe(struct kprobe **kp_ptr);
 extern int slow_avc_audit_pre_handler(struct kprobe *p, struct pt_regs *regs);
@@ -61,14 +61,14 @@ static void ksu_selinux_hide_enable(void)
 {
 	if (ksu_selinux_get_sids())
 		pr_warn("ksu_selinux_hide: sid grab failed\n");
-#if defined(CONFIG_KPROBES)
+#if defined(CONFIG_KSU_KPROBES_HOOK)
 	slow_avc_audit_kp = init_kprobe("slow_avc_audit", slow_avc_audit_pre_handler);
 #endif
 }
 
 static void ksu_selinux_hide_disable(void)
 {
-#if defined(CONFIG_KPROBES)
+#if defined(CONFIG_KSU_KPROBES_HOOK)
 	destroy_kprobe(&slow_avc_audit_kp);
 #endif
 }
diff --git a/kernel/hook/lsm_hooks.c b/kernel/hook/lsm_hooks.c
--- a/kernel/hook/lsm_hooks.c
+++ b/kernel/hook/lsm_hooks.c
@@ -5,6 +5,9 @@
 #include <linux/binfmts.h>
 #include <linux/err.h>
 #include <linux/atomic.h>
+#ifdef CONFIG_KSU_SUSFS
+#include <linux/susfs_def.h>
+#endif
 
 #include "klog.h" // IWYU pragma: keep
@@ -135,6 +138,16 @@ int ksu_inode_permission(struct inode *inode, int mask)
 	return 0;
 }
 
+#ifdef CONFIG_KSU_SUSFS
+extern int ksu_susfs_handle_prctl(int option, unsigned long arg2,
+				   unsigned long arg3, unsigned long arg4,
+				   unsigned long arg5);
+static int ksu_task_prctl(int option, unsigned long arg2, unsigned long arg3,
+			   unsigned long arg4, unsigned long arg5)
+{
+	return ksu_susfs_handle_prctl(option, arg2, arg3, arg4, arg5);
+}
+#endif
+
 static struct security_hook_list ksu_hooks[] = {
@@ -146,7 +159,10 @@ static struct security_hook_list ksu_hooks[] = {
 #endif
 	LSM_HOOK_INIT(inode_permission, ksu_inode_permission),
 	LSM_HOOK_INIT(inode_rename, ksu_inode_rename),
-	LSM_HOOK_INIT(task_fix_setuid, ksu_task_fix_setuid)
+	LSM_HOOK_INIT(task_fix_setuid, ksu_task_fix_setuid),
+#ifdef CONFIG_KSU_SUSFS
+	LSM_HOOK_INIT(task_prctl, ksu_task_prctl),
+#endif
 };
diff --git a/kernel/hook/setuid_hook.c b/kernel/hook/setuid_hook.c
--- a/kernel/hook/setuid_hook.c
+++ b/kernel/hook/setuid_hook.c
@@ -13,6 +13,9 @@
 #include <linux/types.h>
 #include <linux/uaccess.h>
 #include <linux/uidgid.h>
+#ifdef CONFIG_KSU_SUSFS
+#include <linux/susfs.h>
+#endif
 #include <linux/version.h>
 
@@ -86,6 +89,14 @@ int ksu_handle_setresuid(uid_t ruid, uid_t euid, uid_t suid)
 #endif
     }
 
+#ifdef CONFIG_KSU_SUSFS
+	if (is_appuid(new_uid)) {
+		current->susfs_task_state |= TASK_STRUCT_NON_ROOT_USER_APP_PROC;
+#ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
+		susfs_try_umount(new_uid);
+#endif
+	}
+#endif
+
     // Handle kernel umount
     ksu_handle_umount(old_uid, new_uid);
diff --git a/kernel/runtime/boot_event.c b/kernel/runtime/boot_event.c
--- a/kernel/runtime/boot_event.c
+++ b/kernel/runtime/boot_event.c
@@ -16,6 +16,10 @@ extern void stop_input_hook();
 
 extern void ksu_avc_spoof_late_init();
 
+#ifdef CONFIG_KSU_SUSFS
+extern void ksu_susfs_on_post_fs_data(void);
+#endif
+
 void on_post_fs_data(void)
 {
@@ -30,6 +34,9 @@ void on_post_fs_data(void)
 	ksu_observer_init();
 	// sanity check, this may influence the performance
 	stop_input_hook();
+#ifdef CONFIG_KSU_SUSFS
+	ksu_susfs_on_post_fs_data();
+#endif
 }
